spring:
  datasource:
    url: jdbc:mysql://localhost:3306/e_comm_db?createDatabaseIfNotExist=true
    username: root
    password: MySql@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
